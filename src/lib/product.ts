export type Product = {
  name: string;
  tagline: string;
  price: string;
  format: string;
  description: string;
  includes: string[];
  forWhom: string;
};

export type ProductFaqItem = { q: string; a: string };
